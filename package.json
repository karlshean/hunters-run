{
  "name": "hunters-run",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:hr": "npm run -w apps/hr-api start:dev",
    "build:hr": "npm run -w apps/hr-api build",
    "start:hr": "npm run -w apps/hr-api start",
    "test": "echo ok"
  }
}