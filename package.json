{
  "name": "hunters-run",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:hr": "npm run -w apps/hr-api dev",
    "build:hr": "npm run -w apps/hr-api build",
    "test": "echo ok"
  }
}