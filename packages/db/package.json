{
  "name": "@hunters-run/db",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "migrate": "node migrate.js",
    "migrate:reset": "node migrate.js --reset"
  },
  "dependencies": {
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@types/pg": "^8.10.0"
  }
}